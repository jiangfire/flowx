server:
  host: 0.0.0.0
  port: 8080
  mode: "debug"  # debug / release / test
  cors_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"

database:
  host: "localhost"
  port: 5432
  user: "flowx"
  password: "flowx123"
  dbname: "flowx"
  sslmode: "disable"
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_idle_time: 300  # seconds (5 minutes)

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "change-me-in-production"
  expire_hours: 24

log:
  level: "info"  # debug / info / warn / error

llm:
  endpoint: "http://localhost:11434/v1"  # Ollama default
  api_key: ""
  model: "qwen2.5:7b"
  timeout: 30
